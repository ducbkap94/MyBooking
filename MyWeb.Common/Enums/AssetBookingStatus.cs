namespace MyWeb.Common.Enums
{
    public enum DeviceBookingStatus
    {
        Waiting = 0,       // Chờ lấy
        Renting = 1,       // Đang cho thuê
        Returned = 2,      // Đã trả
        Lost = 3,          // Mất
        Broken = 4         // Hư hại
    }


}